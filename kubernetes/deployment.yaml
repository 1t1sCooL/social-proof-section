apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-proof-section-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-proof-section-app
  template:
    metadata:
      labels:
        app: social-proof-section-app
    spec:
      containers:
        - name: social-proof-section-app
          image: social-proof-section-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
